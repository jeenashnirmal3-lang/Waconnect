# Fail2ban filter for Jeenash WA Admin Auth
# Matches failed login logs from Node.js Express server

[Definition]
failregex = ^.*Failed login attempt detected from client IP.*<HOST>.*$
            ^.*Login error: Invalid credentials from <HOST>.*$
ignoreregex =
